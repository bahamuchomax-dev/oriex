// Settings feature — scaffold placeholder.
// App settings. Edit/settings entry points should be consolidated here / in profile edit, not scattered.
export default function Settings() {
  return (
    <section className="feature-placeholder">
      <h2>Settings</h2>
      <p>This screen is not implemented yet. See <code>src/features/settings/README.md</code>.</p>
    </section>
  );
}
