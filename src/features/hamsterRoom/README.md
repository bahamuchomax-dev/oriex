# Hamster Room

3D hamster room (three.js). Needs: a confirmation dialog before a tutorial, hamster naming (persisted), and a self-grooming idle behavior.

**Status:** scaffold only — implement here.

See `docs/rebuild-plan.md` for where this sits in the rebuild order, and
`docs/features.md` for the recovered v6.9 behavior notes.
