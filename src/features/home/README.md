# Home

Landing screen after login. Shows the user's name / XP / streak / role and
cards that navigate to the main areas. Receives `profile` and a `navigate(key)`
callback from `App.jsx`.

**Status:** basic implementation (stage 1).
