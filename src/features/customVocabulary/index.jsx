// Custom Vocabulary feature — scaffold placeholder.
// Shared custom vocabulary at public/data/customVocabulary. NOTE: the seenBy write-back was intentionally removed; do not reintroduce it. Per-user seen state lives in users/{uid}/customSeen.
export default function CustomVocabulary() {
  return (
    <section className="feature-placeholder">
      <h2>Custom Vocabulary</h2>
      <p>This screen is not implemented yet. See <code>src/features/customVocabulary/README.md</code>.</p>
    </section>
  );
}
