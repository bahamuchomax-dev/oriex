// Auth feature — scaffold placeholder.
// Firebase Authentication: sign-in/up, current-user context, and gating the rest of the app. Wraps onAuthStateChanged and exposes the uid used everywhere else.
export default function Auth() {
  return (
    <section className="feature-placeholder">
      <h2>Auth</h2>
      <p>This screen is not implemented yet. See <code>src/features/auth/README.md</code>.</p>
    </section>
  );
}
