# src/components

Shared, presentation-level UI used across features.

- `BottomNav.jsx` — the fixed bottom tab bar driven by App.jsx.

Add genuinely reusable building blocks here (buttons, cards, dialogs, the
profile card, the tutorial dialog, etc.). Feature-specific logic belongs in
`src/features/<name>/` instead.
