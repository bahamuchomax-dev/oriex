# legacy-dist — REFERENCE ONLY

This is the original **v6.9 production build** (minified). It is kept here only
as a reference: UI/wording comparison, recovering string literals, and behavior
checks.

Do NOT develop by editing these files — they are minified and unmaintainable.
All new development happens in `../src/`. See the top-level `README.md` and
`../docs/`.

(The original build's own note is preserved as `README.md` in this folder.)
